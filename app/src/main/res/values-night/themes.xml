<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LostFound" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="accentColor">@color/wine </item>
        <item name="backgroundColor">@color/navy</item>
        <item name="textColorDark">@color/black</item>
        <item name="textColorNormal">@color/light_grey</item>
        <item name="textColorLight">@color/white</item>
        <item name="inputColor">@color/light_grey</item>
        <item name="colorPrimary">@color/wine</item>

        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NoActionBarTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="accentColor">@color/wine </item>
        <item name="backgroundColor">@color/navy</item>
        <item name="textColorDark">@color/black</item>
        <item name="textColorNormal">@color/light_grey</item>
        <item name="textColorLight">@color/white</item>
        <item name="inputColor">@color/light_grey</item>
        <item name="colorPrimary">@color/wine</item>


    </style>
</resources>