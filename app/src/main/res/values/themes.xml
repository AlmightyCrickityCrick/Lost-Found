<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LostFound" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="accentColor">@color/blue </item>
        <item name="backgroundColor">@color/off_white</item>
        <item name="textColorDark">@color/black</item>
        <item name="textColorNormal">@color/black</item>
        <item name="textColorLight">@color/whitest</item>
        <item name="inputColor">@color/whitest</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="drawerColor">@color/whitest</item>
        <item name="cardBackground">@color/whitest</item>
        <item name="android:fontFamily">@font/montserrat</item>

        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NoActionBarTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="accentColor">@color/blue </item>
        <item name="backgroundColor">@color/off_white</item>
        <item name="textColorDark">@color/black</item>
        <item name="textColorNormal">@color/black</item>
        <item name="textColorLight">@color/whitest</item>
        <item name="inputColor">@color/whitest</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="drawerColor">@color/whitest</item>
        <item name="cardBackground">@color/whitest</item>
        <item name="android:fontFamily">@font/montserrat</item>




    </style>
    <style name="roundedImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>